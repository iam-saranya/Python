# Read input
N = int(input())

# Calculate and output the total number of courses
total_courses = 2 * N
print(total_courses)
#This Python program reads an integer `N` from the user, representing the number of languages. It then calculates and prints the total number of courses, which is twice the value of `N`.
